import { CtaBanner } from './cta-banner'
import { CtaSplit } from './cta-split'

// The only static import of these components anywhere — see hero/variants.ts for why.
export const variants = { banner: CtaBanner, split: CtaSplit }
